# CI/CD 加固与 GHCR 镜像流水线 — 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复 deploy.yml 的 check-ci 缺陷，完善部署通知和 secrets 文档（第一阶段），并为 GHCR 预构建镜像流水线打好基础（第二阶段）。

**Architecture:** 第一阶段仅改 deploy.yml 和文档，不涉及应用代码。第二阶段在 CI 中新增 publish job 将镜像推送到 GHCR，生产 compose 增加 image 字段实现 pull 部署，deploy.sh 去掉服务器端 build 步骤。

**Tech Stack:** GitHub Actions, Octokit REST API, Docker, GHCR (ghcr.io), Docker Compose

**Design Spec:** `docs/superpowers/specs/2026-04-16-cicd-design.md`

---

## 文件结构

### 第一阶段（当下实施）

| 操作 | 文件 | 职责 |
|------|------|------|
| 修改 | `.github/workflows/deploy.yml` | 修复 check-ci permissions/filter/paginate + 改善通知 |
| 修改 | `docs/deployment.md` | 追加 GitHub Actions Secrets 章节 |

### 第二阶段（有服务器后）

| 操作 | 文件 | 职责 |
|------|------|------|
| 修改 | `.github/workflows/ci.yml` | 新增 publish job（GHCR build & push） |
| 修改 | `deploy/docker-compose.yml` | backend/worker/scheduler/frontend 加 image 字段 |
| 修改 | `.github/workflows/deploy.yml` | SSH script 加 export IMAGE_TAG |
| 修改 | `deploy/scripts/deploy.sh` | build 替换为 pull |
| 修改 | `deploy/.env.example` | 追加 GHCR_OWNER 注释 |

---

## 第一阶段

### Task 1: 修复 deploy.yml check-ci job

**Files:**
- Modify: `.github/workflows/deploy.yml:16-39`

- [ ] **Step 1: 给 check-ci job 添加 permissions 并重写 script**

将 `.github/workflows/deploy.yml` 的 `check-ci` job 完整替换为：

```yaml
  check-ci:
    runs-on: ubuntu-latest
    permissions:
      checks: read
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref }}
      - name: Verify CI passed for this ref
        uses: actions/github-script@v7
        with:
          script: |
            const ref = '${{ github.event.inputs.ref }}';
            const checkRuns = await github.paginate(github.rest.checks.listForRef, {
              owner: context.repo.owner,
              repo: context.repo.repo,
              ref: ref,
              filter: 'latest',
              per_page: 100,
            });

            const ciBackend  = checkRuns.find(c => c.name === 'backend');
            const ciFrontend = checkRuns.find(c => c.name === 'frontend');

            if (!ciBackend || ciBackend.conclusion !== 'success') {
              core.setFailed(`Backend CI has not passed for ref ${ref}`);
            }
            if (!ciFrontend || ciFrontend.conclusion !== 'success') {
              core.setFailed(`Frontend CI has not passed for ref ${ref}`);
            }
            core.info('CI checks passed');
```

变更点：
1. 新增 `permissions: checks: read`（私有仓库需要此权限调用 `listForRef`）
2. `github.paginate` 替代原来的 `github.rest.checks.listForRef`（自动翻页）
3. `filter: 'latest'`（只取每个 suite 最新 run，排除旧失败记录）
4. `per_page: 100`（避免默认 30 条截断）

- [ ] **Step 2: 验证 YAML 语法**

Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))" && echo OK`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "fix: deploy.yml check-ci 增加 permissions + filter + paginate"
```

---

### Task 2: 改善部署通知 webhook payload

**Files:**
- Modify: `.github/workflows/deploy.yml:56-70`（`Notify deploy result` step）

- [ ] **Step 1: 替换 Notify deploy result step**

将 `deploy.yml` 中 `Notify deploy result` step 的 `run` 部分替换为：

```yaml
      - name: Notify deploy result
        if: always()
        run: |
          STATUS="${{ job.status }}"
          REF="${{ github.event.inputs.ref }}"
          echo "Deploy $STATUS for ref $REF"
          if [ -n "${{ secrets.DEPLOY_NOTIFY_WEBHOOK }}" ]; then
            curl -sf -X POST "${{ secrets.DEPLOY_NOTIFY_WEBHOOK }}" \
              -H "Content-Type: application/json" \
              -d "{
                \"text\": \"Deploy ${STATUS} | ref: ${REF} | by: ${{ github.actor }} | run: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}\"
              }" || true
          fi
```

变更点：webhook body 增加 `run:` 链接字段，方便从通知直接跳转到 Actions 页面定位问题。

- [ ] **Step 2: 验证 YAML 语法**

Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))" && echo OK`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "fix: deploy 通知 webhook 增加 run URL 上下文"
```

---

### Task 3: 在 deployment.md 补充 GitHub Actions Secrets 章节

**Files:**
- Modify: `docs/deployment.md`（在 `## 2. 前置条件` 之后、`## 3. 环境变量` 之前插入新章节）

- [ ] **Step 1: 在 deployment.md 中插入 Secrets 章节**

在 `docs/deployment.md` 的 `## 2. 前置条件` 表格结束后（约第 168 行 `---` 之前），追加：

```markdown

### GitHub Actions Secrets（CI/CD 部署用）

在仓库 Settings → Secrets and variables → Actions 中配置以下 secrets，`deploy.yml` 手动触发部署时使用：

| Secret | 必填 | 说明 |
|--------|------|------|
| `DEPLOY_HOST` | ✅ | 服务器 IP 或域名 |
| `DEPLOY_USER` | ✅ | SSH 登录用户名 |
| `DEPLOY_SSH_KEY` | ✅ | SSH 私钥完整内容（`cat ~/.ssh/id_ed25519`） |
| `DEPLOY_PATH` | ✅ | 服务器上仓库根目录绝对路径（如 `/opt/restock`） |
| `DEPLOY_NOTIFY_WEBHOOK` | 可选 | 飞书 / Slack incoming webhook URL，部署成功或失败时发送通知 |

配置完成后，在 GitHub Actions 页面手动触发 `Deploy` workflow 即可一键部署。
```

- [ ] **Step 2: 验证 deployment.md 无 Markdown 语法问题**

Run: `head -180 docs/deployment.md | tail -20`
Expected: 看到刚插入的 Secrets 表格

- [ ] **Step 3: Commit**

```bash
git add docs/deployment.md
git commit -m "docs: deployment.md 补充 GitHub Actions Secrets 章节"
```

---

## 第二阶段（有服务器后实施）

### Task 4: ci.yml 新增 publish job

**Files:**
- Modify: `.github/workflows/ci.yml`（在文件末尾 `docker-build` job 之后追加）

- [ ] **Step 1: 在 ci.yml 末尾追加 publish job**

在 `.github/workflows/ci.yml` 文件末尾追加：

```yaml

  publish:
    needs: [backend, frontend, docker-build]
    if: github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/restock-backend:sha-${{ github.sha }}
            ghcr.io/${{ github.repository_owner }}/restock-backend:latest
      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/restock-frontend:sha-${{ github.sha }}
            ghcr.io/${{ github.repository_owner }}/restock-frontend:latest
```

- [ ] **Step 2: 验证 YAML 语法**

Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))" && echo OK`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/ci.yml
git commit -m "feat: ci.yml 新增 publish job 推送镜像到 GHCR"
```

---

### Task 5: docker-compose.yml 增加 image 字段

**Files:**
- Modify: `deploy/docker-compose.yml:84-105`（backend service）
- Modify: `deploy/docker-compose.yml:107-130`（worker service）
- Modify: `deploy/docker-compose.yml:132-158`（scheduler service）
- Modify: `deploy/docker-compose.yml:160-182`（frontend service）

- [ ] **Step 1: 为 backend service 添加 image 字段**

在 `deploy/docker-compose.yml` 的 `backend` service 中，在 `container_name: restock-backend` 之后、`build: *backend-build` 之前插入 `image` 行：

```yaml
  backend:
    container_name: restock-backend
    image: ghcr.io/${GHCR_OWNER}/restock-backend:${IMAGE_TAG:-latest}
    build: *backend-build
```

- [ ] **Step 2: 为 worker service 添加 image 字段**

同样，在 `worker` service 的 `container_name: restock-worker` 之后插入：

```yaml
  worker:
    container_name: restock-worker
    image: ghcr.io/${GHCR_OWNER}/restock-backend:${IMAGE_TAG:-latest}
    build: *backend-build
```

注意：worker 使用 **同一个 backend 镜像**（`restock-backend`），不是 `restock-worker`。

- [ ] **Step 3: 为 scheduler service 添加 image 字段**

```yaml
  scheduler:
    container_name: restock-scheduler
    image: ghcr.io/${GHCR_OWNER}/restock-backend:${IMAGE_TAG:-latest}
    build: *backend-build
```

- [ ] **Step 4: 为 frontend service 添加 image 字段**

```yaml
  frontend:
    container_name: restock-frontend
    image: ghcr.io/${GHCR_OWNER}/restock-frontend:${IMAGE_TAG:-latest}
    build:
      context: ../frontend
      dockerfile: Dockerfile
```

- [ ] **Step 5: 验证 YAML 语法和 compose 配置**

Run: `python -c "import yaml; yaml.safe_load(open('deploy/docker-compose.yml'))" && echo OK`
Expected: `OK`

Run: `docker compose -f deploy/docker-compose.yml config --quiet 2>&1 || echo "WARN: 需要设置 GHCR_OWNER 等变量，compose config 可能有警告"`

- [ ] **Step 6: Commit**

```bash
git add deploy/docker-compose.yml
git commit -m "feat: docker-compose.yml 为四个服务添加 GHCR image 字段"
```

---

### Task 6: deploy.yml SSH script 添加 IMAGE_TAG

**Files:**
- Modify: `.github/workflows/deploy.yml:42-58`（deploy job SSH script）

- [ ] **Step 1: 替换 deploy job 的 SSH script**

将 `deploy.yml` 中 `deploy` job 的 `Deploy over SSH` step 的 `script` 替换为：

```yaml
      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            set -euo pipefail
            cd "${{ secrets.DEPLOY_PATH }}"
            git fetch --all --tags
            git checkout "${{ github.event.inputs.ref }}"
            git pull --ff-only origin "${{ github.event.inputs.ref }}"
            export IMAGE_TAG="sha-$(git rev-parse HEAD)"
            bash deploy/scripts/deploy.sh
```

变更点：在 `git pull` 之后、`deploy.sh` 之前插入 `export IMAGE_TAG="sha-$(git rev-parse HEAD)"`。`deploy.sh` 作为子进程自动继承该环境变量。

- [ ] **Step 2: 验证 YAML 语法**

Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))" && echo OK`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "feat: deploy SSH script 增加 export IMAGE_TAG 传递镜像版本"
```

---

### Task 7: deploy.sh 将 build 替换为 pull

**Files:**
- Modify: `deploy/scripts/deploy.sh:48`

- [ ] **Step 1: 替换 deploy.sh 第 48 行**

将 `deploy/scripts/deploy.sh` 中的：

```bash
docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" build backend frontend
```

替换为：

```bash
docker compose --env-file "$ENV_FILE" -f "$COMPOSE_FILE" pull backend worker scheduler frontend
```

不改动其他任何行。`IMAGE_TAG` 由上游 SSH script `export` 注入，`docker compose` 通过 compose 文件中的 `${IMAGE_TAG:-latest}` 读取。

- [ ] **Step 2: 验证脚本语法**

Run: `bash -n deploy/scripts/deploy.sh && echo OK`
Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add deploy/scripts/deploy.sh
git commit -m "feat: deploy.sh 用 pull 替换 build，使用 GHCR 预构建镜像"
```

---

### Task 8: deploy/.env.example 追加 GHCR_OWNER 注释

**Files:**
- Modify: `deploy/.env.example`（在文件末尾追加）

- [ ] **Step 1: 在 .env.example 末尾追加**

在 `deploy/.env.example` 文件末尾追加以下内容：

```

# 第二阶段 GHCR 镜像流水线所需（Phase 2）
# GHCR_OWNER=your_github_username
```

注意：保留注释状态（`#` 前缀），不影响当前部署。用户需要时取消注释并填入实际 GitHub 用户名。

- [ ] **Step 2: 验证文件内容**

Run: `tail -3 deploy/.env.example`
Expected: 看到刚追加的 GHCR_OWNER 注释行

- [ ] **Step 3: Commit**

```bash
git add deploy/.env.example
git commit -m "docs: .env.example 追加 GHCR_OWNER 注释（Phase 2 准备）"
```

---

### Task 9（可选）: deploy.yml 增加 tag 触发

**Files:**
- Modify: `.github/workflows/deploy.yml:1-10`（`on:` 部分）

> 此任务为可选项。仅当团队确认需要"打 tag 自动上线"时实施。

- [ ] **Step 1: 在 deploy.yml 的 on: 中追加 push tags 触发**

在 `deploy.yml` 的 `on:` 块中追加 `push` 触发：

```yaml
on:
  workflow_dispatch:
    inputs:
      ref:
        description: "Git ref to deploy"
        required: true
        default: "main"
  push:
    tags:
      - 'v*'
```

- [ ] **Step 2: 修改 deploy job 的 ref 引用，兼容两种触发方式**

deploy job SSH script 中使用的 `${{ github.event.inputs.ref }}` 在 push 触发时为空。需改为：

```yaml
          script: |
            set -euo pipefail
            REF="${{ github.event.inputs.ref || github.ref_name }}"
            cd "${{ secrets.DEPLOY_PATH }}"
            git fetch --all --tags
            git checkout "$REF"
            git pull --ff-only origin "$REF"
            export IMAGE_TAG="sha-$(git rev-parse HEAD)"
            bash deploy/scripts/deploy.sh
```

`check-ci` job 的 ref 引用也需同步修改：

```javascript
const ref = '${{ github.event.inputs.ref || github.sha }}';
```

- [ ] **Step 3: 验证 YAML 语法**

Run: `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))" && echo OK`
Expected: `OK`

- [ ] **Step 4: Commit**

```bash
git add .github/workflows/deploy.yml
git commit -m "feat: deploy.yml 支持 v* tag 触发自动部署（可选）"
```

---

## 验证清单

完成所有任务后，按此清单验证：

| # | 检查项 | 命令 / 方法 | 通过标准 |
|---|--------|------------|---------|
| 1 | deploy.yml YAML 合法 | `python -c "import yaml; yaml.safe_load(open('.github/workflows/deploy.yml'))"` | 无报错 |
| 2 | ci.yml YAML 合法 | `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` | 无报错 |
| 3 | deploy.sh 语法 | `bash -n deploy/scripts/deploy.sh` | 无报错 |
| 4 | compose 配置 | `docker compose -f deploy/docker-compose.yml config --quiet` | 无报错（需设 GHCR_OWNER） |
| 5 | 后端测试未回归 | `cd backend && python -m pytest tests/ -q -p no:cacheprovider` | 268 passed |
| 6 | check-ci 权限 | GitHub UI 手动触发 Deploy，check-ci step 不报 403 | step 成功 |
| 7 | GHCR 镜像（Phase 2） | push master 后查看 GitHub Packages 页 | 可见 `sha-<hash>` + `latest` tag |
| 8 | pull 部署（Phase 2） | 服务器手动触发 Deploy，查看 `docker compose` 日志 | 无 `build` 输出，只有 `pull` 输出 |
