# ============================================
# 部署环境变量
# 复制为 .env 后填充真实值（.env 不入 git）
# ============================================

# 域名（Caddy 自动申请 Let's Encrypt 证书）
APP_DOMAIN=your-domain.com

# 数据库密码（随机强密码）
DB_PASSWORD=please_change_me_use_strong_password

# 赛狐 API 凭证（联系赛狐 ERP 开通 OpenAPI 权限后获取）
SAIHU_CLIENT_ID=your_client_id
SAIHU_CLIENT_SECRET=your_client_secret

# 首次登录密码（会被 bcrypt 后存入 global_config）
LOGIN_PASSWORD=your_initial_login_password

# JWT 签发秘钥：openssl rand -base64 32
JWT_SECRET=generate_with_openssl_rand_base64_32
