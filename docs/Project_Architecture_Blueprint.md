# Restock System ????

> ?????2026-04-11
> ??????`001-saihu-replenishment` ??
> ??????????????????????????

---

## 1. ????

**Restock System** ???????????????????**???????Layered Monolith?**?????????????????????????? Vue SPA ???

### ??????

1. **??? I/O**???? Web ??FastAPI???????asyncpg???? async/await?????
2. **??????**??? TaskRun ??? Celery/Redis????????
3. **?????**?Worker?Scheduler?Reaper ? FastAPI ??? Python ????????????????
4. **?????**?????????????velocity?sale_days????? `restock_regions`???????????????????
5. **???????**???????????????? N+1 ??
6. **??????????**???????????????????????????????????? / ?? / ???????????????????

### ???

| ?? | ?? |
|---|---|
| ?? | Vue 3.5 / TypeScript 5.7 / Pinia 2 / Vue Router 4 / Element Plus 2.9 / Vite 6 / Vitest 4 / ECharts 6 |
| ?? | Python 3.11+ / FastAPI / SQLAlchemy 2.0 async / Pydantic 2 / APScheduler / httpx / structlog |
| ??? | PostgreSQL 16?asyncpg ???/ Alembic ?? |
| ?? | Docker Compose / Caddy ???? / Nginx ???? |

---

## 2. ????

```
???????????????????????????????????????????????????????????????????????
?                       Frontend (Vue 3 SPA)                          ?
?  views ? components ? stores (Pinia) ? api client (Axios)          ?
???????????????????????????????????????????????????????????????????????
                               ? HTTP + JWT
                               ?
???????????????????????????????????????????????????????????????????????
?                       Backend (FastAPI)                             ?
?                                                                      ?
?  ????????????????????????????????????????????????????????????????? ?
?  ? API Layer (app/api)                                           ? ?
?  ? REST ??: auth / data / suggestion / sync / config / monitor ? ?
?  ????????????????????????????????????????????????????????????????? ?
?                         ?                                           ?
?  ????????????????????????????????????????????????????????????????? ?
?  ? Schema Layer (app/schemas) ? Pydantic DTO                     ? ?
?  ????????????????????????????????????????????????????????????????? ?
?                         ?                                           ?
?  ????????????????????????????????????????????????????????????????? ?
?  ? Business Layer                                                ? ?
?  ? ??????????????????????????????????????????????????????????? ? ?
?  ? ? engine/   ? sync/      ? services/   ? tasks/           ? ? ?
?  ? ? 6 ???  ? ??????? Excel ??  ? ??+??+worker ? ? ?
?  ? ??????????????????????????????????????????????????????????? ? ?
?  ? ????????????? ?3.6 ????????            ? ?
?  ????????????????????????????????????????????????????????????????? ?
?                         ?                                           ?
?  ????????????????????????????????????????????????????????????????? ?
?  ? Integration Layer (app/saihu) ? ?? HTTP ???              ? ?
?  ????????????????????????????????????????????????????????????????? ?
?                         ?                                           ?
?  ????????????????????????????????????????????????????????????????? ?
?  ? Model Layer (app/models) ? SQLAlchemy ORM                     ? ?
?  ????????????????????????????????????????????????????????????????? ?
?                         ?                                           ?
?  ????????????????????????????????????????????????????????????????? ?
?  ? DB Layer (app/db) ? AsyncSession Factory                      ? ?
?  ????????????????????????????????????????????????????????????????? ?
?                         ?                                           ?
?  ????????????????????????????????????????????????????????????????? ?
?  ? Core (app/core) ? logging / exceptions / security / timezone ? ?
?  ????????????????????????????????????????????????????????????????? ?
???????????????????????????????????????????????????????????????????????
                               ? async SQL
                               ?
                     ???????????????????????
                     ?  PostgreSQL 16      ?
                     ?  20+ tables         ?
                     ?  (?? + ?? + ??)?
                     ???????????????????????
                               ?
                               ? sync_*
                               ?
                     ???????????????????????
                     ?  Sellfox (?? API) ?
                     ?  ??/??/??/?? ?
                     ???????????????????????
```

**????**?????????????????????? Pydantic DTO?API ? ???? ORM ????? ? DB??

---

## 3. ??????

### 3.1 ???????app/engine?

**??**???????????????????????????? `draft` ?????????????SKU ????????????SKU ? ?? ? ????????? step ????????????????? API?

**6 ????**?

| Step | ?? | ?? | ?? | ?? |
|---|---|---|---|---|
| 1 | `step1_velocity.py` | ? 30 ????????? | `velocity[sku][country]` | ???????7??0.5 + 14??0.3 + 30??0.2???? `source='????'` ? `package_status!='has_canceled'` ??????? `global_config.restock_regions` ?????????????????? |
| 2 | `step2_sale_days.py` | ?? + ?? + velocity + SKU ???? | `sale_days[sku][country]` | `(available + reserved + in_transit) / velocity`?????????????? SKU ???????????????????????? velocity ?????????????????? SKU ???????????????velocity?0 ?? |
| 3 | `step3_country_qty.py` | velocity + ?? + ???????? | `country_qty[sku][country]` | `effective_target_days = target_days + max(demand_date - today, 0)`?`max(0, ceil(effective_target_days ? velocity - (available + reserved + in_transit)))` |
| 4 | `step4_total.py` | country_qty + velocity + ???? + safety_stock_days | `purchase_qty[sku]` | `max(0, ?country_qty ? (local.available + local.reserved) + ceil(?velocity ? safety_stock_days))`?`?country_qty` ?? Step 3 ????????`?velocity` ????????? `restock_regions` ???`buffer_days` ?????? |
| 5 | `step5_warehouse_split.py` | country_qty + ?????? + ????? + ???? + ??????? | `warehouse_breakdown[country][wh_id]` | ???? `source='????'` ? `package_status!='has_canceled'` ??????????? `max(quantity_shipped - refund_num, 0)`??????? `order_header.postal_code`???????????????????????????????????????????????????????????????????????????? `restock_regions`?????????????????????? tied ????????? floor + ??????????????????? |
| 6 | `step6_timing.py` | sale_days + lead_time + country_qty | `urgent` + `restock_dates` | `urgent` ????????? `sale_days <= lead_time_days`?`restock_date[sku][country] = today + int(sale_days[sku][country]) ? lead_time_days`????????????? sale_days ??? `null` |

**?????**?`EngineContext` ?? `target_days`?`buffer_days`?`lead_time_days`?`safety_stock_days`?`restock_regions`?`eu_countries` ?????????? `demand_date`?runner ??? `demand_days=max(demand_date - today, 0)` ??? Step 3 ???????????`buffer_days` ??????????????????????? `purchase_qty` ? `restock_dates` ???`restock_regions` ??????????????`UK` ???? ISO ????? `GB`?`global_config.eu_countries` ?????????????????????????????????????`global_config_snapshot` ?????????? `demand_date` ?????

**SKU ?????**?`backend/app/engine/sku_mapping.py` ?????????? `sku_mapping_rule` / `sku_mapping_component`?????????? `inventory_snapshot_latest`?`in_transit_record` ????????`sku_mapping_component.group_no` ??????????? `commodity_sku` ??? `group_no` ???? AND??? `group_no` ? OR?`A=2*B` ???? `floor(B/2)` ???`A=1*B+2*C` ???? `min(floor(B/1), floor(C/2))` ???`A=B ? C ? D` ?????????????????`A=B+C+D ? E+F+G` ?????????????????????????????????????????????? SKU?????????? SKU ?????????????????????? SKU ??????????????????????????? `A=B+C+D` ? `A=E+F+D` ????????????????????????????????????????Step 2 ????????? `velocity[sku][country]` ???????Step 4 ?? SKU ??? velocity ???????????????????????????????????????Step 2 ????????????? ID ?????????????????????????????? SKU?Step 4 ????????????????????????????????? SKU ????????????????????????? SKU ??? SKU ????????

**?? SKU ???**?`backend/app/services/physical_item.py` ?? `sku_to_group_key` / `members_by_group_key`????????????????`physical_item_group` ????????????? SKU???????? SKU?runner ???? SKU ???? Step 1 / Step 5 ???????????????? Step 2 / Step 4 ????????????????????? SKU ?????????

**???**??????????????? `pg_advisory_xact_lock(7429001)` ???runner ??? `restock_dates[country] <= demand_date` ????????????????????? Step 3 ?????? `restock_regions` ??????? `purchase_qty <= 0` ???????? 0????? SKU?????????????????????????-only ?????? `country_breakdown` / `warehouse_breakdown` / `allocation_snapshot` / `restock_dates` ???`total_qty=0`?`urgent=false`???????? `None`????? `draft`??????????????????????????????? `draft`??? `suggestion` / `suggestion_item`??? `procurement_item_count`?`restock_item_count`??? `calc_engine_job` ? `global_config.suggestion_generation_enabled` ??? OFF?

**????**?`velocity_snapshot`?`sale_days_snapshot`?`allocation_snapshot`?`global_config_snapshot` ?? JSONB ????? `velocity_snapshot` / `sale_days_snapshot` ???? SKU ?????`global_config_snapshot.demand_date` ?????????`suggestion_item.purchase_qty` ???????`country_breakdown` / `warehouse_breakdown` ???????`restock_dates` ???????????? Excel ??????????????

### 3.2 ??????app/sync?

**??**???? API ????????????????????????????

**??**??????????? sync job????????????????????????????

```python
@register("sync_inventory")
async def sync_inventory_job(ctx: JobContext) -> None:
    await mark_sync_running(db, JOB_NAME)
    try:
        async for raw in list_inventory_items(...):  # Saihu ????
            await _upsert_inventory(db, raw, warehouse_country_map)
            if count % 100 == 0:
                await db.commit()
        await mark_sync_success(db, JOB_NAME, started)
    except Exception as exc:
        await mark_sync_failed(db, JOB_NAME, str(exc))
        raise
```

**?? UPSERT**??? `pg_insert(...).on_conflict_do_update(...)` ?????? SELECT ? INSERT/UPDATE ????

**????**??? job ? `sync_state` ???????????????????

**???????**?`sync_product_listing` ??????? job????? `backend/app/saihu/endpoints/commodity.py` ???? SKU ????? `/api/commodity/pageList.json`??? `state` ? `isGroup` ???? `sku` UPSERT ? `commodity_master`??????????? listing ?? `/api/order/api/product/pageList.json` ?? `product_listing`?????????sellerSku ?? 7/14/30 ??????????????????????? SKU ?? `sku_config(enabled=true)`?????? `enabled`?`lead_time_days`????????????????????????? `state` ????/???????????????`run_engine` ???? `sku_config.enabled=true` ? SKU?

**EU ???????????**????????????????????????? `global_config.eu_countries` ?????????? `EU`????? `original_*` ??????????????????????????????????? `trim + uppercase + ?????? + ?????`??? `UK` ?????? ISO ?? `GB`?????????????? `marketplace` ?????????????????? `ZZ` ??????????????????????????????????? `eu_countries` ???? `EU` ?? `original_country_code` ????????????? `eu_countries` ???????????????? `backfill_eu_country_mapping()` ?????? `order_header`?`inventory_snapshot_latest`?`in_transit_record`????????? `original_*` ???????????????????????????????? EU ?????????? `EU` ? `original_* = ??????`??????????????? `original_*`??????????????? API?

**??????**?`GET /api/config/country-options` ?????????????????????????? `country_code/original_country_code`??? `country`??? `country/original_country`??? `target_country/original_target_country`????????????????? `UK` ??? `GB` ??????? `builtin`?`observed`?`can_be_eu_member` ? `unknown_country_codes`????????????????????????? EU ????????????EU ???????? `EU` ? `ZZ`???????? `GB - ??`?`CZ - ??`?`RO - ????`?????????????? `AT - ???`?`CH - ??`?`CY - ????`?`DK - ??`?`EE - ????`?`FI - ??`?`LT - ???`?`LV - ????`?`MT - ???`?`SI - ?????`?

**??????**?`backend/app/core/timezone.py` ? `country_to_tz()` ????????????????? `UK` ?? `GB` ? `Europe/London`?? `EU`?`ZZ` ?????????`BUILTIN_COUNTRY_NAMES` ?????????? `COUNTRY_TO_TIMEZONE` ??? IANA ????????????? tripwire??????????????????????????????? warning?

**????????**?`sync_order_list` ????????????????????????? `/api/packageShip/v1/getPackagePage.json`????? `purchaseDateStart/purchaseDateEnd`?????????????? 12 ????????????`pageSize=200`???????????? `shopIdList`????????????? `order_header` / `order_item`?`source='????'`?`order_platform=platformName`?`shop_name=shopName`?`package_sn/package_status/postal_code` ???????`postal_code` ??? `address.postalCode`????????????????? `address` ???????? `order_header.postal_code`?`country_code/original_country_code` ???? `marketplace`???????? `ZZ`??? `amazonOrderId` ???? `packageSn` ?? `shop_id + amazon_order_id + source + package_sn` ???????? `orders` ??????`items` ? `items.amazonOrderId` ?????`order_item.commodity_sku` ??? `items.commoditySku`?????? `items.sellerSku` ????????????????`order_item.seller_sku` ????? `sellerSku`?`items.quantityOrdered` ???? `quantity_ordered` ? `quantity_shipped`?`refund_num=0`???????? `source in ('???','???')` ?????????????????????????????

**??????**?`sync_out_records` ??????????????? `in_transit_record` / `in_transit_item`????????????????? `warehouseId`?`updateTime`?`type/typeName`?`commodityId`?`perPurchase`???????????????????????`target_country` ?????????????? `20260410??-??-??-??-???` ? `US`????????????????? `targetFbaWarehouseId -> warehouse.country`???????????????????? `target_country` ????????????????????????

**???????????**?????????????????????SKU???????????????? `sync_order_detail`??? `refetch_order_detail`?????????????????? job ???????? endpoint ???????`order_detail` ? `order_detail_fetch_log` ???????????????????????????????

### 3.3 ???????app/tasks?

**???**?`task_run`

```sql
CREATE TABLE task_run (
  id BIGSERIAL PRIMARY KEY,
  job_name VARCHAR NOT NULL,
  status VARCHAR NOT NULL,  -- pending / running / success / failed / skipped / cancelled
  dedupe_key VARCHAR,
  trigger_source VARCHAR,   -- scheduler / manual / api
  priority INT DEFAULT 0,
  payload JSONB,
  worker_id VARCHAR,
  started_at TIMESTAMPTZ,
  finished_at TIMESTAMPTZ,
  heartbeat_at TIMESTAMPTZ,
  lease_expires_at TIMESTAMPTZ,
  current_step VARCHAR,
  step_detail TEXT,
  total_steps INT,
  error_msg TEXT,
  result_summary TEXT
);

-- ????????? pending/running ???
CREATE UNIQUE INDEX uq_task_run_active_dedupe 
  ON task_run (dedupe_key) 
  WHERE status IN ('pending', 'running');

-- Worker ?????? pending?
CREATE INDEX ix_task_run_pending_priority 
  ON task_run (status, priority, created_at) 
  WHERE status = 'pending';

-- Reaper ?????? running?
CREATE INDEX ix_task_run_lease 
  ON task_run (lease_expires_at) 
  WHERE status = 'running';
```

**??????**?

```
?????????????????    enqueue    ????????????    claim     ??????????
?   Scheduler   ????????????????? task_run ???????????????? Worker ?
? (APScheduler) ?               ?   (DB)   ?              ? (poll) ?
?????????????????               ????????????              ??????????
       ?                              ?                        ?
       ?                              ? reap expired           ? heartbeat
       ? cron/interval                ?                        ?
       ?                        ????????????                   ?
       ??????? register jobs ????  Reaper  ?????????????????????
                                ????????????
```

- **Scheduler**?APScheduler ???/cron ????? `enqueue_task()`?????????
- **Worker**?? 2 ??????? `FOR UPDATE SKIP LOCKED` ???? pending ??????? + ????
- **Reaper**?? 60 ??? `lease_expires_at < now()` ? running ?????? failed?worker ?????
- **Heartbeat**?? 30 ??? `lease_expires_at`??? `heartbeat ? 2 < lease_minutes ? 60`

**??????**?`sync_shop` ?? 03:00 ???`sync_warehouse` ?? 03:30 ???`daily_archive` ?? 02:00 ???`retention_purge` ?? 04:00 ???`retry_failed_api_calls` ? 5 ?????`sync_product_listing`?`sync_inventory`?`sync_out_records`?`sync_order_list` ? `global_config.sync_interval_minutes` ?????`GET /api/sync/scheduler` ? API ????????? API ??????? APScheduler????? job trigger ?????????????????? `PROCESS_ENABLE_SCHEDULER=true` ? scheduler ???

**????**?`TaskRun.current_step / step_detail / total_steps / result_summary` ? worker ????????? `TaskProgress` ???? `/api/tasks/{id}`?`calc_engine` ?????????????? JSON ???`generated`?`suggestion_id`?`demand_date`??? `reason`???????????????????????????????????? `totalPage` ???? P / N ????????????????? 2026-04-20 ??worker ? heartbeat?????? success/failed ?????? `status='running' + worker_id` ???????? reaper ????? `TaskLeaseLostError` ??????????
**????????**?`refresh_dashboard_snapshot` ???? TaskRun ?????????????????????????? `build_dashboard_payload()` ?? `dashboard_snapshot` ?????????????????????????
**????????**?`retry_failed_api_calls` ??? TaskRun job?? APScheduler ? 5 ????????? `dedupe_key=job_name` ????????? `api_call_log` ????????? `40019` ????????????????????????????????
**???????**?`app/tasks/access.py` ???? TaskRun ?????????/????????? `POST /api/tasks` ???????????????????????? ?3.6 ?????????????

### 3.4 ??????app/saihu?

**????? + ????? + Token ?? + ?? + ??**?

```
???
  ?
  ?
endpoints/xxx.py  ? ???????????????
  ?
  ?
SaihuClient.post()  ? ???? + ??
  ?
  ??? TokenManager.get_token()  ? ???? + 5 ?????
  ?
  ??? rate_limit.get_limiter(endpoint)  ? per-endpoint QPS
  ?
  ??? httpx.AsyncClient.post()  ? ?? HTTP
  ?
  ??? ApiCallLog INSERT  ? ???????
```

**????**?tenacity??
- ?????1s ? 2s ? 4s ? 10s
- ????`SaihuRateLimited`?40019??`SaihuNetworkError`
- ????`SaihuBizError`???????
- **????**?`SaihuAuthExpired`?40001?? ?????**??**???? token ??????
- **????**????? `40019` ???? `request_payload` ???????? `retry_failed_api_calls` ???????? `endpoint + request_payload` ?????????? `retry_source_log_id` ????????????????????

**??**?
```python
_ENDPOINT_RATE_OVERRIDES = {}
# ?? 1 QPS per endpoint????????????? QPS ??
```

`retry_failed_api_calls` ???? limiter ???????????????? 1 QPS endpoint ?? 1.5 ?????????? `/api/packageShip/v1/getPackagePage.json` ?? `sync_order_list`?????????????? `sync_order_list` / `sync_all`?????????????????????????????????

`/api/commodity/pageList.json` ? `/api/order/api/product/pageList.json` ??? `sync_product_listing`??? `40019` ????????????????????????????????????

**Token ??**??? `get_token()` ????????? Future??????????????

### 3.5 ?????

| ??? | ?? | ?? |
|---|---|---|
| **??** | JWT HS256?24h ?????? `sub="owner"` | `core/security.py`, `api/deps.py` |
| **??** | `BusinessError` ???NotFound/Unauthorized/ValidationFailed/...?+ `SaihuAPIError` ????? exception handler ? JSON | `core/exceptions.py`, `main.py:113-131` |
| **??** | structlog + contextvars ?? `request_id`?dev ConsoleRenderer / prod JSONRenderer | `core/logging.py`, `core/middleware.py` |
| **???** | `RequestLoggingMiddleware` ?? method/path/status/duration_ms??? X-Request-Id ??? | `core/middleware.py` |
| **????** | ??? IP ????????????????????????????? key | `core/rate_limit.py` |
| **??** | ?? UTC????????`parse_saihu_time` ? marketplace_id ???????? | `core/timezone.py` |
| **??** | `pydantic-settings` ?????/`.env` ???`get_settings()` ?? | `config.py` |

**RBAC ????**????????? `backend/app/core/permissions.py`????????? `backend/app/api/auth_roles.py` ??? `expand_permission_dependencies()`????????????????`edit`?`operate`?`manage`?`delete`?`export`?`refresh`?`new_cycle`??????? `view` ?????????????????????????????`GET /api/auth/roles/{role_id}/permissions` ???????????????????? `role_permission` ???????????? active ???????????????????????????????? bump `sys_user.perm_version`?

### 3.6 ????????app/services + app/api/snapshot.py?

**??**????????????????? ? ??????/???? ? ????? Snapshot + Excel ?? ? ???????????????? API????????????? Excel ????????

**???**?

| ? | ?? | ???? |
|---|---|---|
| `suggestion_snapshot` | ??????? snapshot??? `snapshot_type`?`version`?`exported_by`?`exported_from_ip`?`global_config_snapshot`?`item_count`?`generation_status`?`file_path`?`file_size_bytes`?`download_count` | FK `suggestion_id`?`UNIQUE(suggestion_id, snapshot_type, version)` |
| `suggestion_snapshot_item` | snapshot ?????????????? `purchase_qty`??????? `total_qty` / `country_breakdown` / `warehouse_breakdown` / `restock_dates` | FK `snapshot_id` ON DELETE CASCADE |
| `suggestion_item` | ?? draft ????????????????????? | `purchase_qty` ??????`country_breakdown` / `warehouse_breakdown` ??????`restock_dates` ??????????? Excel ???`procurement_export_status` / `procurement_exported_snapshot_id` / `procurement_exported_at`?`restock_*` ?? |

**API ??**?

| ?? | ?? |
|---|---|
| `POST /api/suggestions/{id}/snapshots/procurement` | ??????????????? `purchase_qty > 0` |
| `POST /api/suggestions/{id}/snapshots/restock` | ??????????????? `sum(country_breakdown) > 0` |
| `POST /api/suggestions/{id}/snapshots` | ?????? 410 Gone????????????? |
| `GET /api/suggestions/{id}/snapshots?type=procurement|restock` | ???????????? type ???? |
| `GET /api/snapshots/{snapshot_id}` | ??????????? |
| `GET /api/snapshots/{snapshot_id}/download` | ?? Excel????????? |

**????**?

1. `SELECT ... FOR UPDATE` ??????????? `global_config(id=1)`?
2. ??????? `draft`????????????? `snapshot_type` ??????????
3. ? `(suggestion_id, snapshot_type)` ??????? `version`?
4. INSERT `suggestion_snapshot(generation_status='generating')` ? `suggestion_snapshot_item`?
5. `excel_export.py` ??????????????????? + ????????????? + SKU?? + SKU??? + SKU??????????????? `global_config_snapshot.demand_date` ????????????/?????????????????????????????? `restock_dates` ????????????????????? `restock_dates`?
6. ???????????????? `{type}_export_status='exported'`?`{type}_exported_snapshot_id`?`{type}_exported_at`?
7. ?? `suggestion_snapshot.generation_status='ready'`?`file_path`?`file_size_bytes`?

**????**?? Excel ??????????? snapshot ?? `failed`???????????/????????????????????

## 4. ??????

### 4.1 ????

```
src/
??? views/           # ???18 ??
?   ??? LoginView / WorkspaceView / NotFoundView
?   ??? SuggestionListView / SuggestionDetailView / HistoryView
?   ??? data/        # 6 ????
?   ??? SyncConsoleView / GlobalConfigView / ZipcodeRuleView / ApiMonitorView / PerformanceMonitorView
??? components/      # ?????
?   ??? AppLayout / PageSectionCard / MobileRecordList / SkuCard / StatusTag / TablePaginationBar / TaskProgress
?   ??? charts/      # BaseChart (ECharts wrapper)
?   ??? dashboard/   # Dashboard ????
?   ??? sync/        # ??????
??? api/             # API ?????????????
??? stores/          # Pinia ???auth / sidebar / task?
??? router/          # ?? + ????
??? utils/           # ?????format / tableSort / countries / warehouse / status / monitoring / storage / download / ...?
??? styles/          # ?????tokens.scss + element-overrides.scss?
??? config/          # ??????????
??? main.ts
```

`components/dashboard/` ?? `DashboardChartCard` ???????????????????????? footer ??????????????????????????????????? footer ????????????????????`AppLayout`?`PageSectionCard`?`TablePaginationBar` ? `element-overrides.scss` ???????????????????????????????????dialog/????????????

**??????????**?`frontend/src/api/snapshot.ts` ???? `app/api/snapshot.py`??? / ?? / ?? / ?????????? `frontend/src/utils/download.ts`?`triggerBlobDownload`?? `SuggestionDetailView` ?"?? Excel"???"?????"?????????`api/snapshot.ts` ? `downloadSnapshotBlob` ??? `Content-Disposition` ??????? `triggerBlobDownload`??? blob ???????????????

### 4.2 ????

**?? shadcn/ui Zinc ??**?? `src/styles/tokens.scss` ????
- **??**?50+ ??????? `#18181b`?zinc-900?
- **??**?4px ???`$space-1` ? `$space-16`?
- **??**???????????? Google Fonts??? `deploy/Caddyfile` ???? `fonts.googleapis.com` / `fonts.gstatic.com`????????????????
- **??**?4 / 6 / 8 / 12 / pill
- **??**?card / popup ??

`element-overrides.scss` ?? Element Plus ? `--el-*` CSS ???????? UI ????????????

### 4.3 ????

| Store | ?? | ??? |
|---|---|---|
| `useAuthStore` | token, user, isAuthenticated | localStorage??? JSON ?????? |
| `useSidebarStore` | isCollapsed, expandedCategories | localStorage??? JSON ?????? |
| `useTaskStore` | tasksById, polling | ???????? |

**??????? 3 ? store**?????????? view ? `ref/reactive` ??

### 4.4 API ???

`frontend/src/api/*` ????????

| ?? | ?? |
|---|---|
| `suggestion.ts` | ???????????????? PATCH????`Suggestion` ?? `procurement_item_count` / `restock_item_count` / `procurement_snapshot_count` / `restock_snapshot_count`?`SuggestionItem` ?? `purchase_qty` ??????? |
| `snapshot.ts` | `createProcurementSnapshot()`?`createRestockSnapshot()`?`listSnapshots(id, type?)`???????`SnapshotOut.snapshot_type` ? `procurement` / `restock` |
| `config.ts` | `GlobalConfig` ?? `safety_stock_days`?`eu_countries`?`restock_regions` ??????`CountryOptionsResponse` ?? `GET /api/config/country-options` ????????`GenerationToggle` ?? `can_enable` / `can_enable_reason` |
| `engine.ts` / `task.ts` | ???????????? `demand_date` ? `POST /api/engine/run`?`task.ts` ???? `getTask()` ? `listTasks()`??????????? `calc_engine` ? `pending` / `running` ??????? |

?? API ??????? `api/client.ts` ?? Bearer token??????????? 401 ?????

### 4.5 ?????

**???????????????? + ?????**?

```typescript
const rows = ref<T[]>([])                    // ?????
const total = ref(0)
const filters = reactive({ ... })            // ????
const page = ref(1)
const pageSize = ref(50)

async function reload() {
  const resp = await listData({
    page: page.value,
    page_size: pageSize.value,
    ...filters,
  })
  rows.value = resp.items
  total.value = resp.total
}
```

??????????
- `DataOrdersView.vue`?????????????????? `item_count` / `has_detail`????? SKU / ??????????????????????? `GET /api/data/order-platforms` ??????????????????????????????????????????????????????????? `source='????'`?????? `package_sn` ??????????
- `HistoryView.vue`??????????? `GET /api/suggestions` ? `items/total/page/page_size`?????? `getSuggestionDisplayStatusMeta(status, snapshot_count)` ?? 4 ??????`??? / ??? / ??? / ??`?????????? `display_status=pending|exported|archived|error`??????? `snapshot_count` ????????????????? `items` ? `total` ???`canDelete(row)` ??? `row.snapshot_count === 0`???????? `frontend/src/utils/status.ts::deriveSuggestionDisplayStatus`?`SuggestionListView` ? `SuggestionDetailView` ??? tag ????????????????
- `DataProductsView.vue`?????? `listSkuOverview()` ?? SKU???????????????`/api/data/sku-overview` ? `commodity_master + sku_config` ???????????????????????????????listing ?????????????? listing ? SKU ????
- `DataInventoryView.vue`?????? `GET /api/data/inventory/warehouse-groups` ???????????????????????? `is_package` ??????????? SKU??? listing ?? SKU ? SKU ?????? SKU???????????? / ??? / ????????
- `DataOutRecordsView.vue`??????? SKU???????????????????????????

**???????**?

```typescript
const rows = ref<T[]>([])                    // ?????
const total = ref(0)
const page = ref(1)
const pageSize = ref(50)

async function reload() {
  const resp = await listOrders({
    page: page.value,
    page_size: pageSize.value,
    shop_id: filters.shop || undefined,
    sort_by: sortState.value.prop,
    sort_order: sortState.value.order,
  })
  rows.value = resp.items
  total.value = resp.total
}
```

?????????????????????????? `item_count` / `has_detail` ????????????????????????????????????????????????????????????????

**???????**???????????????????????????????????????????????????

### 4.6 ?????

??????? `frontend/src/router/index.ts`???? `RouteMeta.permission` ??????????????????? Tab?

| ??? | ????? | ??? |
|---|---|---|
| `/restock/current` | `/restock/current/procurement` | `procurement` ???????`restock` ?????? |
| `/restock/suggestions/:id` | `/restock/suggestions/:id/procurement` | `procurement` ????????`restock` ??????? |
| `/restock/history` | `/restock/history/procurement` | `procurement` ???????`restock` ?????? |

`SuggestionTabBar` ??????????????????????????????????? header?????????/????????????`authGuard` ? `beforeEach` ??????????????? `/login?redirect=<origin>` ? `/403`?

### 4.7 ??????

| ?? | ?? | ???? |
|---|---|---|
| `PageSectionCard` | ?????????`title` + `#actions` slot | ????? |
| `MobileRecordList` | ????????????????loading ????? | ??????????????????????? |
| `SkuCard` | ??????? + ?? + SKU + blocker ??? | ???????????? |
| `StatusTag` | ??????? StatusMeta ??? | ?????? |
| `TablePaginationBar` | ????v-model ?? currentPage ? pageSize???????????? | ?????? |
| `TaskProgress` | ???????????? `/api/tasks/{id}`???????????/???????????????? `job_name` ??????????? | ??????? |

**????????**?
- `src/utils/monitoring.ts` ????????????????????? `endpoint`????? `request/resource` ???????? tooltip ???????
- `src/composables/useResponsive.ts` ???? `isMobile` / `isTablet` ??????????????????????????? composable?????????? resize ??
- `ApiMonitorView`?`PerformanceMonitorView`?`sync/FailedApiCallTable` ????????????????????????????tooltip ????

**???????**?
- `WorkspaceView` ???????????`restock_sku_count` ????????????? `total_qty > 0` ??? SKU ??`no_restock_sku_count` ????? SKU ??`risk_country_count` ???????? `restock_regions` ?????????????
- ???????????????????????????????? `dashboard_snapshot.payload.country_risk_distribution` ? SKU+????????????????? `restock_regions=[]` ???????????? `["EU"]` ???? `EU`
- ?????SKU?????????????? `sale_days`??????????? `restock_regions` ???????????? SKU ????????????? SKU ??
- ????????????? `country_restock_distribution`???????????? `country_breakdown` ???????????????????

**????????????**?

| ?? | ?? |
|---|---|
| `SuggestionListView` | ?? `PageSectionCard.actions` ?????????? tag ????? `????`???????????????????????????????????`loadToggle()` + `loadActiveEngineTask()` ? `onMounted` / `onActivated` ?????????? `calc_engine` ???????????? `TaskProgress` ?????????????????????????????????????????????????????????? |
| `SuggestionDetailView` | ?? `export_status='pending'` ??? ? ??? Excel??????? `POST /api/suggestions/{id}/snapshots` + `GET /api/snapshots/{id}/download` blob ???????? `finally` ? `await load()` ?????????????????????????????????????????`PageSectionCard`?6 ??? `version` ???????????????????????????? fail-close ?????`SkuCard` ???? `:blocker`??? prop ????????????????? `isEditable` ?? `export_status !== 'exported'`??? snapshot ?????????? ?9.5 Don'ts?? |
| `GlobalConfigView` | ???????????`el-switch` ?????? ON ?? `ElMessageBox` ?????????? draft??`PATCH` ?????????????? `config:edit` ??????????????????? |
| `SkuMappingRuleView` | ????? > ???? > ???????? `GET /api/config/sku-mapping-rules` ?????????? SKU / ?? SKU???????????????????????Excel/CSV ??? Excel ??????? `group_no` ???????????? `?` ?????????????????????????????????? SKU??????????????????????????? |
| `HistoryView` | ?? ?4.5 ? ???? 3 ????? + ??????`canDelete` ?? `snapshot_count === 0`? |

---

## 5. ??????

### ?? 1?????????

```text
???????????????????
  ?
POST /api/engine/run { demand_date: "YYYY-MM-DD" }
  ?
api/sync.py: ????????????????enqueue_task("calc_engine", "manual")
  ?
task_run ? INSERT status=pending, dedupe_key="calc_engine"
  ?
worker ???? ? calc_engine_job
  ?
run_engine ?? global_config?target_days / buffer_days / lead_time_days / safety_stock_days / restock_regions / eu_countries???? payload.demand_date ??????
  ?
pg_advisory_xact_lock(7429001)
  ?
6 ?????velocity ? sale_days ? country_qty??? target_days + demand_days?? purchase_qty ? warehouse_split ? urgent/restock_dates
  ?
??????????? suggestion_item?????????????? no_suggestion_needed
  ?
???? INSERT suggestion + suggestion_item[]?????/?? item_count ??????? demand_date?
  ?
?????????? suggestion_generation_enabled=false
  ?
?? TaskProgress ???? ? ????/?? Tab
```

### ?? 2??? / ????????

```text
???????? Tab ????
  ?
???POST /api/suggestions/{id}/snapshots/procurement
???POST /api/suggestions/{id}/snapshots/restock
  ?
?? suggestion + item + global_config
  ?
? snapshot_type ????????? version
  ?
INSERT suggestion_snapshot(snapshot_type, generation_status='generating')
  ?
INSERT suggestion_snapshot_item[]??????????????
  ?
????/???? Excel ??????
  ?
UPDATE suggestion_item SET procurement_* ? restock_* ????
  ?
GET /api/snapshots/{snapshot_id}/download ????
```

### ?? 3?????????????? draft?

```text
????????????????
  ?
GET /api/config/generation-toggle ?? can_enable / can_enable_reason
  ?
PATCH /api/config/generation-toggle { enabled: true }
  ?
?????????????/?? item_count>0???????????? 1 ? snapshot
  ?
UPDATE suggestion SET status='archived', archived_trigger='admin_toggle', archived_by=<user>, archived_at=now
  ?
UPDATE global_config SET suggestion_generation_enabled=true, generation_toggle_updated_by/at=...
  ?
????? calc_engine ??? draft??????????? OFF
```

## 6. ?????

### 6.1 ???

| ? | ?? | ???? / ?? |
|---|---|---|
| `global_config` | ?????????? `target_days`?`buffer_days`?`lead_time_days`?`safety_stock_days`?`restock_regions`?`eu_countries`?`suggestion_generation_enabled`?`generation_toggle_updated_by / generation_toggle_updated_at`???????? | `CHECK id=1`?`safety_stock_days` ?? 1?90?`restock_regions=[]` ?????????????`eu_countries` ????????? ISO ????????? `EU` ? `ZZ` |
| `commodity_master` | ?? SKU ??? | ?? `sku`??? `commodity_id/name/state/is_group/img_url/purchase_days/child_skus/last_sync_at`???????????????????????????????? |
| `sku_config` | SKU ?? / ??????? | `commodity_sku` ???`20260504_1000` ????????????????????????? SKU ?? `enabled=true`?????????? |
| `sku_mapping_rule` | ?? SKU ????? SKU ????? | `commodity_sku` ???`enabled=false` ??????????? |
| `sku_mapping_component` | ??????? | `rule_id + inventory_sku` ??????????????? `inventory_sku`?`group_no > 0`?`quantity > 0`??? `group_no` ????? AND ????? `group_no` ?? OR ???? |
| `physical_item_group` / `physical_item_sku_alias` | ?? SKU ??????? | ????? `primary_sku`??? SKU ?????`enabled=false` ?????????????? |
| `warehouse` | ???/??????? | `country` ??????????????????????? |
| `order_header` / `order_item` | ???????? | `order_header.source='????'` ????????`order_platform` ??????`package_sn/package_status/shop_name/postal_code` ?????????????`UNIQUE(shop_id, amazon_order_id, source, package_sn)` ????????`country_code` ????????`original_country_code` ?? EU ??????? `shop_id + purchase_date`?`order_platform + purchase_date`?`order_status + purchase_date`?`package_status + purchase_date` ??? |
| `order_detail` / `order_detail_fetch_log` | ??????????? | ????????????????????????????????????? |
| `product_listing` | ???????? | `marketplace_id` ????? 2 ?????? `EU`?`original_marketplace_id` ?? EU ????? |
| `inventory_snapshot_latest` | SKU ? ?????? | `country` ????????`original_country` ?? EU ??????`warehouse_id + commodity_sku` ?? |
| `in_transit_record` / `in_transit_item` | ?? / ??????? | `target_country` ????????`original_target_country` ?? EU ????? |
| `suggestion` | ???? | `status IN ('draft','archived','error')`?`procurement_item_count` / `restock_item_count` ??????????? SKU ? |
| `suggestion_item` | ?????? | `purchase_qty` ??????`country_breakdown` / `warehouse_breakdown` ??????`restock_dates` ??????????? Excel ???????????? `*_export_status`?`*_exported_snapshot_id`?`*_exported_at` |
| `suggestion_snapshot` | ???????3.6? | `snapshot_type IN ('procurement','restock')`?`UNIQUE(suggestion_id, snapshot_type, version)`?`generation_status IN ('generating','ready','failed')` |
| `suggestion_snapshot_item` | ???????? | FK `snapshot_id` ON DELETE CASCADE??? `purchase_qty`?`restock_dates` ????? JSONB |
| `excel_export_log` | Excel ???? | ?? snapshot?????IP?User-Agent?????? |
| `task_run` | ?????? | `dedupe_key + active status` partial unique index |
| `dashboard_snapshot` | ???????? | ???? dashboard payload?????????? |
| `api_call_log` | ?? API ????? 40019 ???? | `request_payload` ???????`retry_status` ?? `queued/resolved/permanent/unsupported`?`retry_source_log_id` ????????? |
| `login_attempt` | ????????? | `source_key` PK |

#### `zipcode_rule`?????????

- ???`country`, `prefix_length`, `value_type`, `operator`, `compare_value`, `warehouse_id`, `priority`
- `operator ? {=, !=, >, >=, <, <=, contains, not_contains, between}`?`String(10)`?CHECK ?? `operator_enum`?
- `compare_value: String(200)`?`between` ?????????? `lo-hi` ????????????? 20 ??`hi ? 10^prefix_length - 1`?
- ? `(country, priority)` ????????????????????"???"
- **tied ??**??????? `priority` ??????????????tied ?????????????? priority ???? operator ??

### 6.2 ??????

**????**?Partial Index??
- `uq_task_run_active_dedupe`????????????????????
- `ix_task_run_pending_priority`?Worker ??????
- `ix_task_run_lease`?Reaper ????????
- `ix_api_call_log_retry_queue`??? `retry_failed_api_calls` ?? `40019 + queued + request_payload` ???????
- `ix_suggestion_item_urgent`????????

**JSONB ??**?`country_breakdown`?`warehouse_breakdown`?`velocity_snapshot`?`sale_days_snapshot`?`global_config_snapshot`?`allocation_snapshot`?`payload`?`request_payload`??? `global_config_snapshot` ??? `restock_regions` ??????`api_call_log.request_payload` ???????? `40019` ??????????????? JSONB ??????? GIN ???

### 6.3 ????

- ???Alembic???????? `db/base.py` ? NAMING_CONVENTION?
- ???`backend/alembic/versions/`
- ???`YYYYMMDD_HHMM_description.py`
- ???`alembic upgrade head`??????? deploy ???????
- ?????????????????? head ? `20260425_1420`?? revision ????? marker???? schema??????????? `20260425_1420` ?????

---

## 7. ????

### 7.1 Docker Compose ??

```
????????????
?  Caddy   ? :443  ? HTTPS terminator?Let's Encrypt ????
????????????
     ?
     ??????? /api/*, /docs, /healthz, /readyz ?? ????????????
     ?                                            ? backend  ? :8000
     ??????? ?????? ?? ??????????????      ? FastAPI  ?
                             ?  frontend  ?      ????????????
                             ?   nginx    ?            ?
                             ??????????????            ?
                                                       ?
                                                 ????????????
                                                 ?    db    ? :5432
                                                 ? Postgres ?
                                                 ????????????
                                                       ?
                                                       ?
                                            ???????????????????????
                                            ?                     ?
                                       ???????????          ??????????????
                                       ? worker  ?          ? scheduler  ?
                                       ? ? FastAPI ??   ?  ? FastAPI  ?
                                       ???????????          ??????????????
```

**????**?
- db: 1GB
- backend: 512MB
- worker: 512MB
- scheduler: 512MB
- frontend (nginx): 256MB
- caddy: 128MB

### 7.2 ??????

???????????????????????

```env
# backend ?????? HTTP
PROCESS_ENABLE_WORKER=false
PROCESS_ENABLE_REAPER=false
PROCESS_ENABLE_SCHEDULER=false

# worker ???????
PROCESS_ENABLE_WORKER=true
PROCESS_ENABLE_REAPER=true
PROCESS_ENABLE_SCHEDULER=false

# scheduler ??????
PROCESS_ENABLE_WORKER=false
PROCESS_ENABLE_REAPER=false
PROCESS_ENABLE_SCHEDULER=true
```

???????? Worker?Scheduler ???????????

### 7.3 ?????deploy/scripts/?

| ?? | ?? |
|---|---|
| `deploy.sh` | ??????? env ? ?? ? ?? ? ???? ? ???? ? smoke check ? ?????? |
| `migrate.sh` | ???????? |
| `pg_backup.sh` | PostgreSQL ?? |
| `restore_db.sh` | ????? |
| `rollback.sh` | ?????????? |
| `validate_env.sh` | ????????? |
| `smoke_check.sh` | ????????/healthz, /readyz? |

`deploy.sh` ?????? `IMAGE_TAG` ??????????? HEAD ?? `sha-<commit>`?? CI ??? GHCR ??????????GitHub Actions ?????????

### 7.4 ????

- **`GET /healthz`**????????? `{"status":"ok"}`
- **`GET /readyz`**?????????
  1. ???????`SELECT 1`?
  2. Worker ?????????
  3. Reaper ?????????
  4. Scheduler ?????????

---

## 8. ???????ADR ???

### ADR-1??? async

- **??**?FastAPI + SQLAlchemy async + httpx + asyncio
- **??**???? I/O ???????? + ?? HTTP ?? + ???? I/O??? CPU bound ??
- **??**??????? async/await??????? I/O

### ADR-2??? TaskRun ?? Celery

- **??**?`task_run` ? + ??? worker/scheduler/reaper
- **??**?
  - ?? Redis/RabbitMQ broker?????
  - TaskRun ??????????????????dedupe?????
  - ?????????????????????
- **??**??? Celery ???Flower ??????????
- **????**????????????????? task ??????????????

### ADR-3?????????????

- **??**?`pg_advisory_xact_lock` ????????
- **??**?
  - ??????????????
  - ?????????????????
  - ??????????????
- **??**????? PostgreSQL ??

### ADR-4???????????

- **??**??????????????????????????? server-side ????????????????????????????
- **??**?
  - ?????????? 5000 ??????????
  - ??????????????????????? `item_count` / `has_detail`
  - ??????????????????????????
- **??**?
  - ?????????????????
  - ???????????? ID????????????
- **????**???????????????????????????

### ADR-5?PageSectionCard ???????????

- **??**?????????? `PageSectionCard`?`#title` + `#actions` slot?
- **??**??????????? header ??
- **??**?
  - `SuggestionDetailView`???? header ??????? `el-card`?
  - `GlobalConfigView` ???

### ADR-6???????????????

- **Status**: Superseded by Plan A 2026-04-19?? ?3.6 / PROGRESS.md ?3.49???????????????? ADR ???????????????????
- **Replacement decision**?`load_in_transit` ????????????????????`in_transit_record` + `in_transit_item`?? `is_in_transit=true` ???????????????????????? Excel ?? + ???????????????????????????????????????? OFF ???? ? ??????????? ON ???? draft??????????????????
- **?????????**?`load_in_transit` ??????????????????
- **?????????**?
  - ???????????????? sync ??????
  - ?????????0 ????
- **?????????**????? 90 ?????????????????

---

## 9. ???????

### 9.1 ????????

1. **??**?? `app/api/data.py` ???????????? `list_orders`?
2. **?? API**?? `src/api/data.ts` ?? TypeScript ?? + ????
3. **????**?
   - ? `src/views/data/` ?? `DataXxxView.vue`
   - ?? `PageSectionCard` + `#actions` slot ???
   - ?"???? + filteredRows + pagedRows" ??
   - ? `formatShortTime`?`formatUpdateTime`?`warehouseTypeLabel` ?????????????????????????????????/????????? `formatUpdateTime` ?? `YYYY-MM-DD HH:mm`
4. **??**?? `src/router/index.ts` ????
5. **??**?? `src/config/navigation.ts` ?????

### 9.2 ????????

1. ? `app/tasks/jobs/` ? `app/sync/` ?? `xxx_job.py`
2. ? `@register(JOB_NAME)` ?????
3. ?????`async def xxx_job(ctx: JobContext) -> None`
4. ???/???? `mark_sync_running/success/failed`???? sync ??
5. ? `app/tasks/scheduler.py` ? `_register_jobs` ???????
6. ??????? `enqueue_task(db, job_name=JOB_NAME, trigger_source="manual")`

### 9.3 ?????? Step

**???**??? 6 ????????????????? step ????????????????????
- **???? step**???? step ?????
- **???? job**????????????? task??? `suggestion_item` ?????

### 9.4 ?????? API ??

1. ? `app/saihu/endpoints/` ?? `xxx.py`
2. ???? `inventory.py`?
   ```python
   async def list_xxx(page_size: int = 100, ...) -> AsyncIterator[dict]:
       client = get_saihu_client()
       page_no = 1
       while True:
           result = await client.post(ENDPOINT, body)
           for row in result["data"]["rows"]:
               yield row
           if ????: return
           page_no += 1
   ```
3. ???? QPS?? `rate_limit.py` ? `_ENDPOINT_RATE_OVERRIDES` ??
4. ????sync job ??? iterator + ?? UPSERT

### 9.5 ?????Don'ts?

- **??**? API ???? SQL?? ORM ?????
- **??**??? step ????? API?step ??? DB/??
- **??**? sync job ???????sync ??"?? ? ??"
- **??**?? TaskRun ????????????
- **??**? `suggestion_item.procurement_export_status='exported'` ?????????????? `restock_export_status='exported'` ???????????????? snapshot ??????????
- **??**?? `global_config.suggestion_generation_enabled` ?????? `run_engine`??? calc ????????????????? OFF
- **??**??? store ????????? auth/sidebar/task????????? view ?

---

## 10. ????

### 10.1 ??

| ?? | ?? | ???? |
|---|---|---|
| ???? | pytest + pytest-asyncio | ??? step?zipcode_matcher?????????????? |
| ???? | pytest + TEST_DATABASE_URL | ???????????????? skip? |

**??**?`cd backend && pytest`

**??????**?
- `tests/unit/test_engine_step1.py` ~ `test_engine_step6.py`
- `tests/unit/test_zipcode_matcher.py`
- `tests/unit/test_sign.py`????????

### 10.2 ??

| ?? | ?? | ???? |
|---|---|---|
| ???? | Vitest 4 | API ??????????????Pinia store |

**??**?`cd frontend && npm run test`

---

## 11. ?????

### 11.1 ????

```bash
# 1. ???
cp deploy/.env.dev.example deploy/.env.dev
docker compose --env-file deploy/.env.dev -f deploy/docker-compose.dev.yml up -d db

# 2. ??
cd backend
python -m venv .venv
source .venv/bin/activate  # ? .venv/Scripts/activate (Windows)
pip install -e ".[dev]"
cp .env.example .env  # ?? SAIHU_CLIENT_ID ?
alembic upgrade head
uvicorn app.main:app --reload

# 3. ??
cd frontend
npm install
npm run dev
```

### 11.2 ????

```bash
# ??
cd backend
ruff check .
black --check .
mypy app
pytest

# ??
cd frontend
npm run lint
npm run type-check
npm run test
npm run build
```

### 11.3 ????

- ???????
- ?????? conventional commits?`feat:` / `fix:` / `refactor:` / `docs:` / `test:` / `chore:`
- PR ?????? lint + test + build

---

## 12. ????

### 12.1 ?????

- **CLAUDE.md**?????????????????AI ??????????
- **docs/**?????????????????????????????
- **???**??????????Source of Truth?

### 12.2 ??????

1. ??????????????????
2. ??????????????????????
3. ??????
4. ???????
5. ?????
6. ???????????
7. ????????????????

---

## ?? A???????

| ?? | ?? |
|---|---|
| ???? | `backend/app/engine/runner.py` |
| ???? | `backend/app/tasks/queue.py` |
| Worker ?? | `backend/app/tasks/worker.py` |
| ??? | `backend/app/tasks/scheduler.py` |
| ????? | `backend/app/saihu/client.py` |
| Token ?? | `backend/app/saihu/token.py` |
| ???? | `backend/app/core/exceptions.py` |
| ???? | `backend/app/core/timezone.py` |
| ???? | `frontend/src/router/index.ts` |
| ??? | `frontend/src/components/AppLayout.vue` |
| ???? | `frontend/src/styles/tokens.scss` |
| ?????? | `frontend/src/components/PageSectionCard.vue` |

## ?? B???????

**??** (`backend/.env`)?
```
DATABASE_URL=postgresql+asyncpg://user:pass@host:5432/db
SAIHU_BASE_URL=https://openapi.sellfox.com
SAIHU_CLIENT_ID, SAIHU_CLIENT_SECRET
LOGIN_PASSWORD, JWT_SECRET??? 32 ????? 64 ??????, JWT_EXPIRES_HOURS=24
PROCESS_ENABLE_WORKER=true
PROCESS_ENABLE_REAPER=true
PROCESS_ENABLE_SCHEDULER=true
WORKER_LEASE_MINUTES=2
WORKER_HEARTBEAT_SECONDS=30
REAPER_INTERVAL_SECONDS=60
```

**??** (`frontend/.env`)?
```
VITE_API_PROXY_TARGET=http://localhost:8000
```

---

## ????

| ?? | ?? | ?? PROGRESS ?? |
|---|---|---|
| 2026-05-04 | ???????? SKU ?? `commoditySku || sellerSku`??? `seller_sku` ??????????????? | PROGRESS.md ?3.100 |
| 2026-05-04 | ??????????? `GET /api/data/order-platforms` ???????`order_header(order_platform, purchase_date)` ???????????????????????????? | PROGRESS.md ?3.99 |
| 2026-05-04 | ?? SKU ????????????`physical_item_group` ?? `primary_sku`?`physical_item` ??????????????????? SKU ???? | PROGRESS.md ?3.98 |
| 2026-05-04 | ?? `sku_config` ????????????????? SKU ?? `enabled=true`??????????????????? | PROGRESS.md ?3.96 |
| 2026-05-03 | ????????????? `/api/packageShip/v1/getPackagePage.json`?`order_header` ?????????????Step 1 / Step 5 ??????????????? job ???? endpoint ?????????? | PROGRESS.md ?3.91 / ?3.92 |
| 2026-05-03 | ?????????????? `marketplace`???????? `ZZ`??????????????/????????????????? | PROGRESS.md ?3.94 |
| 2026-05-03 | ????????????? `AT/CH/CY/DK/EE/FI/LT/LV/MT/SI` ??? label ? IANA ???`ZZ` ????????????? `marketplace` ???????????????????? | PROGRESS.md ?3.93 |
| 2026-05-03 | SKU ??????????????`sku_mapping_component` ????? `inventory_sku` ??? `rule_id + inventory_sku`?Step 2 ????? velocity ?????????Step 4 ???? velocity ????????????????? | PROGRESS.md ?3.90 |
| 2026-05-03 | ???????? SKU ??? `/api/commodity/pageList.json`??? `commodity_master` ??`sync_product_listing` ????????? listing???????????????? | PROGRESS.md ?3.89 |
| 2026-05-02 | SKU ???????????`sku_mapping_component.group_no` ?? AND??? OR?Step 2/Step 4 ?????????????????????????????????? | PROGRESS.md ?3.88 |
| 2026-04-30 | Step 5 ?????????????????????????????????????????????? | PROGRESS.md ?3.87 |
| 2026-04-29 | ????????????????????????????????????? bump `perm_version`??????????? active ??? | PROGRESS.md ?3.85 |
| 2026-04-29 | Step 5 matched ?????? floor + ???????????????????????? item?????????????? / ???? | PROGRESS.md ?3.84 |
| 2026-04-29 | ?????????????EU ????????????????????????????????? `UK` ?????? ISO ?? `GB`?EU ?????????????????????????????? | PROGRESS.md ?3.83 |
| 2026-04-29 | ????????????????????? `date_end`?????? `purchase` ?????? 6 ???? | PROGRESS.md ?3.82 |
| 2026-04-28 | ????????????? + ??????????????? 2 ??????????EU ????????? `eu_countries` ???`EU` / `ZZ` ???? EU ??? | PROGRESS.md ?3.79 |
| 2026-04-27 | ???? / ???????? `40019` ??????????? `request_payload`?? 5 ?????????????? `resolved`??? 5 ?? `permanent` | ?3.75 |
| 2026-04-27 | ???????????????`is_package` ? `product_listing.commodity_sku` ????????????????? / ????????????????? | ?3.74 |
| 2026-04-27 | ?? SKU ?????????????????????????????? SKU ?????????????? SKU ?? | ?3.76 |
| 2026-04-26 | ???????????`demand_date` ?? Step 3 ?????????`restock_dates` ???????????? Excel ????????????? | ?3.71 |
| 2026-04-25 | ???????????????????????????? `docs/` ???? | ?3.64 |
| 2026-04-24 | ??????-only ????????????????? `purchase_qty > 0` ??? SKU??????????/???????? | ?3.63 |
| 2026-04-24 | ???????????????????????`restock_dates` ???????????? Excel ??? | ?3.62 |
| 2026-04-24 | ? `demand_date` ?????runner ?? `restock_dates[country] <= demand_date` ???????????? 2026-04-26 ??????????? | ?3.61 / ?3.71 |
| 2026-04-24 | ?????? `purchase_date`?step6 ??? `urgent` / `restock_dates`???????? Excel ???????????????????????????30????? | ?3.59 |
| 2026-04-23 | ?????????`buffer_days` ??????????? `purchase_date`????? `purchase_qty`????????????? | ?3.54 |
| 2026-04-22 | Audit Stage 3 P0 ????engine step4 clamp `purchase_qty >= 0` + DB CheckConstraint?`docs_enabled()` production ???? env?CI ? postgres service ? integration tests ???`.gitignore` ? `*.exe` / `*.lnk` ?? commit | PROGRESS.md ???? |
| 2026-04-21 | ??/???? + ???? + EU ?? + ?? Tab ?? | ?3.53 |
| 2026-04-20 | Full audit ????????? / ???? / fail-close ?? / dev ?????? | ?3.52 |
| 2026-04-19 | Plan A ?? + ?????????? ? Excel ?? + Snapshot ??? | ?3.49 / ?3.50 / ?3.51 |
| 2026-04-17 ??? | ???? / ???? / ?????? | ?3.44 ??? |

?????????????? `PROGRESS.md` ?????

---

**????????????????** ??????????????????????????
